<!DOCTYPE html>
<html>
  <head>
    <title>Blog :: index</title>
    <link type="text/css"
          href="https://glyph.twistedmatrix.com/theme/css/main.css?d3be937b"
          rel="stylesheet">
  </head>

  <body>
    <div class="container">
      <section id="content">
        {% for post in posts %}
        <div class="post-block">
          <div class="article-header">
            <div class="article-title">{{ post.title }}</div>
            <p>
              <span class="date">{{ post.date }}</span>
            </p>
          </div>

          <div class="entry-content">
            {{ post['body'] }}
          </div>
        </div>
        {% endfor %}
      </section>
    </div>
  </body>
</html>
